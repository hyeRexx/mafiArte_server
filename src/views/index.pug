doctype html
html(lang="en")
    head
        base(herf="/")
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Noom
        link(rel="stylesheet" href="https://unpkg.com/mvp.css")
    body 
        header
            h1 Mafia 
            h4 "What are they drawing...?" 
        main 
            #welcome
                form#name
                    input(placeholder="nickname", required, type="text")
                    button Save
                form#roomname
                    input(placeholder="room name", required, type="text")
                    button Enter Room 
                    h4 Open Rooms :
                    ul
            #room
                dev#container
                    //- canvas#myCanvas(width="300", height="300")
                h3 
                ul 
                form#msg
                    input(placeholder="message", required, type="text")
                    button Send
                div#call
                    div#myStream
                        video#myFace(autoplay, playinline, width="300", height="300", style="background-color: red;")
                        button#mute Mute
                        button#camera Turn Camera Off
                        select#cameras
                    div#othersStream
            //- dev#container
            //-     canvas#myCanvas(width="300", height="300")
        footer 
            a team 248                           
        //- script(src="https://cdn.jsdelivr.net/gh/over-engineer/Socket.io-whiteboard@1.1.1/lib/whiteboard.js")                          
        script(src="../public/js/board.js")                          
        script(src="/socket.io/socket.io.js")
        script(src="/public/js/index.js")